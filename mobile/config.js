// Mobile runtime config — update this value after deploying the serverless function.
window.LLM_ENDPOINT = 'https://YOUR_DEPLOYED_URL/api/generate';
// Optionally set a client key to be sent in 'x-api-key' header if your function requires it.
window.LLM_CLIENT_KEY = '';
